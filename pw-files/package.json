{
  "name": "playwright-studio",
  "version": "1.0.0",
  "description": "Playwright 录制 + AI 优化 + 执行 + 报告 一体化工具",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "install:pw": "npx playwright install chromium"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "@playwright/test": "^1.44.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
