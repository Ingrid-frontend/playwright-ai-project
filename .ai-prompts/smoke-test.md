# 冒烟测试（给 AI 的提示词）

## 目标

验证：登录功能可用，失败提示正确，成功后能进入首页/仪表板。

## 期望输出

输出 **Markdown 步骤清单**（每步可直接转成 Playwright 代码），包含：

- 前置条件（环境/账号来源）
- 成功登录路径
- 失败路径（错误密码）
- 失败路径（空字段）

## 约束

- 优先使用语义化定位符（Role/Label）
- 避免写死不稳定 CSS 选择器
- 如可行，关键节点补充截图对比点（用于回归）
