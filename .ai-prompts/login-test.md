# 登录测试（给 AI 的提示词）

## 目标

覆盖登录的成功与失败分支，并确保断言稳定。

## 期望输出

输出 **Playwright 测试代码**（可直接放入 `tests/e2e/`），至少包含：

- 成功登录
- 错误密码失败
- 空字段校验失败

## 约束

- 优先使用语义化定位符（Role/Label）
- 对“成功”使用 URL/关键元素可见性断言
- 对“失败”断言错误提示文案或表单校验状态
