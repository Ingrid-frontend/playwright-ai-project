# 环境变量配置模板
# 复制此文件为 .env 并填入真实的值

# 环境选择（dev / uat / stage9084 / stage）
NODE_ENV=stage

# 测试账号信息（可选，如果配置了 datasource/accounts.json 则优先使用配置文件）
TEST_USERNAME=your-account@example.com
TEST_PASSWORD=your-password

# 其他配置
CI=false
